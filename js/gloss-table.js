(function () {
    var template = document.getElementById('table-template').innerText;

    function joinText(pair) {
        return pair.gloss.join('');
    }
    
    function toTable(glossList) {
    }

    return toTable;
}());
